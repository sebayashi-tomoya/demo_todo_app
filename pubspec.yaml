name: demo_todo_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter_hooks:
  hooks_riverpod:
  flutter_riverpod:
  freezed_annotation:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  riverpod_annotation: ^2.3.5
  uuid:

dev_dependencies:
  freezed:
  riverpod_generator:
  build_runner:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
