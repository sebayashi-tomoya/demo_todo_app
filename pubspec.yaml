name: demo_todo_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter_riverpod:
  freezed_annotation:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  freezed:
  build_runner:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
